import './globals.css';
export const metadata = { title: 'Global Inventory — Operations workspace', description: 'Inventory, branch handovers and reporting in one connected workspace.' };
export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
